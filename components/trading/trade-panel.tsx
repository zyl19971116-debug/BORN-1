"use client";import {useState} from "react";import {Area,AreaChart,ResponsiveContainer,Tooltip} from "recharts";const points=Array.from({length:30},(_,i)=>({x:i,y:40+Math.sin(i/2)*8+i*1.4+(i%4)*2}));export function TradePanel({ticker}:{ticker:string}){const [side,setSide]=useState("BUY");return <div className="mt-10 grid gap-5 lg:grid-cols-[1.5fr_1fr]"><div className="card p-5"><div className="mb-4 text-sm font-bold">${ticker} PRICE</div><div className="h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={points}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#b8ff3d" stopOpacity={.35}/><stop offset="1" stopColor="#b8ff3d" stopOpacity={0}/></linearGradient></defs><Tooltip contentStyle={{background:"#111",border:"1px solid #333"}}/><Area type="monotone" dataKey="y" stroke="#b8ff3d" fill="url(#g)"/></AreaChart></ResponsiveContainer></div></div><div className="card p-5"><div className="grid grid-cols-2 rounded-xl bg-white/5 p-1">{["BUY","SELL"].map(x=><button onClick={()=>setSide(x)} className={`rounded-lg p-2 text-sm font-bold ${side===x?"bg-white/10 lime":""}`} key={x}>{x}</button>)}</div><label className="mt-5 block text-xs text-white/50">PAY AMOUNT<input className="mt-2" placeholder="0.00 ETH"/></label><div className="mt-3 flex justify-between text-xs text-white/40"><span>SLIPPAGE 0.5%</span><span>BALANCE 2.48 ETH</span></div><button className="btn btn-primary mt-5 w-full">CONNECT WALLET TO {side}</button><p className="muted mt-3 text-center text-[10px]">MOCK TRADING · DEX INTERFACE RESERVED</p></div></div>}
