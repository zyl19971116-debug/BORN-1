"use client";import {useEffect,useState} from "react";
export function Countdown(){const [s,setS]=useState(42*60+18);useEffect(()=>{const t=setInterval(()=>setS(v=>v<=0?3599:v-1),1000);return()=>clearInterval(t)},[]);const m=String(Math.floor(s/60)).padStart(2,"0"),sec=String(s%60).padStart(2,"0");return <div><div className="text-[10px] font-bold tracking-[.18em] text-white/40">ENDS IN</div><div className="font-mono text-3xl font-bold tracking-tight md:text-4xl">{m}<span className="lime animate-pulse">:</span>{sec}</div>{s<=60&&<div className="mt-1 text-[10px] font-bold text-red-400">FINAL MINUTE</div>}</div>}
