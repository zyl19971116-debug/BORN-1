import Link from "next/link";import {Meme} from "@/types";export function BornCard({meme}:{meme:Meme}){return <article className="card p-4"><div className="flex gap-4"><div className="h-16 w-16 rounded-2xl bg-cover" style={{backgroundImage:`url(${meme.image})`,backgroundPosition:meme.imagePosition,backgroundSize:meme.image.includes("grid")?"300% 300%":"cover"}}/><div><h3 className="font-bold">{meme.name}</h3><p className="text-xs text-white/40">${meme.ticker} · {meme.bornAgo}</p><div className={`mt-2 text-sm font-bold ${(meme.change||0)>=0?"lime":"text-red-400"}`}>{(meme.change||0)>0?"+":""}{meme.change}%</div></div></div><div className="mt-5 grid grid-cols-2 gap-4 border-t border-white/10 pt-4 text-xs"><div><span className="muted">MARKET CAP</span><b className="mt-1 block">{meme.marketCap}</b></div><div><span className="muted">24H VOLUME</span><b className="mt-1 block">{meme.volume}</b></div></div><Link className="btn mt-4 w-full" href={`/meme/${meme.id}`}>TRADE</Link></article>}
