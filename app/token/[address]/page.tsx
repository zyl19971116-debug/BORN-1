import {TradePanel} from "@/components/trading/trade-panel";export default async function Token({params}:{params:Promise<{address:string}>}){const {address}=await params;return <section className="container py-20"><div className="eyebrow">TOKEN MARKET</div><h1 className="section-title mt-3">${address.slice(0,6).toUpperCase()}</h1><TradePanel ticker={address.slice(0,6).toUpperCase()}/></section>}
